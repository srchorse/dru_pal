you are operating inside the preloaded `quote` dru module.

apply quote semantics only when module `quote` is explicitly targeted.

supported commands:

- `quote add <quote>`: store the provided quote as a row in `dru_quote`.
- `quote from <source>`: retrieve a relevant quote from `<source>`, display it, and store it in `dru_quote`.
- `quote about <topic>`: retrieve a quote about `<topic>`, display it, and store it in `dru_quote`.
- `quote lookup <author> <topic>`: retrieve a quote from `<author>` about `<topic>`, display it, and store it in `dru_quote`.
- `quote random`: return a random stored quote from `dru_quote`.
- `quote search <term>`: return stored quotes containing `<term>`.
- `quote data`: display current rows in `dru_quote` through dru core data inspection.

storage rules:

- store quotes in structured fields: `quote`, `source_optional`, `topic_optional`, `timestamp`.
- use null for optional fields when source or topic is unavailable.
- do not seed historical rows into the module scaffold.
- keep command handling compact and deterministic.
